import { AccountTypeScreen as AccountTypeScreenComponent } from '../../screens/AccountTypeScreen';

export const AccountTypeScreen = () => {
  // For now, the component handles its own state
  // We'll need to modify it to accept callbacks later
  return <AccountTypeScreenComponent />;
};
